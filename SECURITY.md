# 安全策略

## 支持范围

当前维护版本为 v1.2.x。请勿在公开 Issue 中提交 Cookie、Token、设备标识、二维码、数据库或包含用户正文的日志。

## 报告漏洞

请通过 GitHub Security Advisory 私密报告。报告应包含受影响版本、复现步骤、影响范围和
脱敏日志；完整 Cookie 始终保留在用户本地。

## 安全基线

- 默认 dry-run，主动刷帖默认关闭且要求人工审核；无审核真实发送必须由用户同时关闭两个安全开关后显式启用。
- 凭证只写入 AstrBot 的插件数据目录，不进入插件配置、WebUI 返回或日志。
- 图片 URL 仅接受公开 HTTPS 地址；本地文件、内网和重定向到内网会被拒绝。
- 普通小黑盒用户不自动映射为 AstrBot 管理员。

## 无审核主动发送

`proactive_feed.dry_run=false` 且 `review_required=false` 是受支持的高风险显式模式。该模式
跳过候选审核，但不绕过真实发送安全边界：主动事件先持久化，发送继续使用楼层锁、事件级
发送记录、重复发送闸门、近期评论核对和 `send_unknown` 状态。主动刷帖、安全开关、每轮和
每日 AI 请求上限的默认值均保持不变；适配器启动时会记录无审核真实发送警告。
