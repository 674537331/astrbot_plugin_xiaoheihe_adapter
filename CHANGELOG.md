# 更新日志

## v1.2.14 - 2026-08-08

- 修复 `reply` / `mention` 新通知超过 `polling.max_pages_per_poll` 后旧消息边界长期落在扫描窗口之外、每轮重复扫描首页并持续输出“暂不推进消息边界”警告的问题；超出单轮页数的未完成区间现在持久化为独立回填任务，实时通知游标可以继续跟上最新消息。
- 历史通知回填继续复用现有 SQLite 入站事件唯一约束和队列去重；插件重启后从持久化 offset 恢复，回填期间若又出现一批超过单轮窗口的新通知会安全回退扫描起点并保留最旧边界，避免为追求实时性跳过中间通知。
- 修复身份增强在 `on_llm_request` 二次转换时漏掉 `recent_participants` 的实际链路缺口；长楼层压缩现在确实携带程序提取的“昵称 + UID”只读身份锚点，并在压缩后由本地代码重新附回，当前发言人与直接回复对象继续保留未压缩原文。
- 图片转述和楼层压缩等辅助 Provider 失败后按错误类型进入 60–300 秒冷却，期间自动跳过已知故障 Provider 并走备用/确定性降级，不会熔断 AstrBot 主对话回复；首次故障与手动恢复写入结构化日志，Plugin Page 显示冷却用途、Provider、剩余时间和错误，并可手动“结束冷却”。
- 原帖图片转述结果增加 30 分钟、最多 256 条的内存文字缓存，不缓存图片字节；同楼层并发上下文 miss 合并为一次请求，默认上下文 TTL 从 300 秒降为 60 秒，通知已携带原帖快照时楼层读取不再重复请求原帖，同一事件相同图片域名也只做一次 DNS 校验。
- 事件分发直接复用账号 HTTP Client 已加载的凭证，不再为每条评论同步重读凭证文件；无图片、短楼层、不需要辅助 Provider、未安装/未调用 Grok 的路径不增加模型调用。本版本仍不修改 AstrBot system prompt 或人格内容。
- 数据库迁移升级至 v8：v7 保存通知跨轮回填边界，v8 为自身评论/完成事件补充清理索引；已完成事件、发送记录、自身评论、终态主动候选和日计数按现有去重保留期回收，单次日常清理可连续消化多批积压，避免元数据无限增长。
- 移除三个从未接入运行时的误导配置 `max_image_size_mb`、`max_total_image_size_mb`、`image_cache_soft_limit_mb`。插件仍限制每事件图片数量并只传安全公开 HTTPS URL；图片字节下载/压缩继续由 AstrBot/Provider 媒体链路负责。

## v1.2.13 - 2026-08-08

- 修复评论区歪楼或切换话题时，长原帖和大量楼层背景容易压过当前评论的问题；被动楼层明确按“当前原生用户消息 > 直接回复对象 > 最近楼层对话 > 原帖背景”组织焦点，当前问题能独立理解时禁止强行拉回原帖主题。
- 直接回复对象从通知引用关系或楼层父评论中单独提取为高相关性原文；当前入站评论和直接回复对象从普通楼层历史中去重，避免同一内容重复放大。
- 在确定性焦点路由上增加来源感知的 LLM 语义压缩：长被动楼层分别压缩原帖与最近讨论，并派生当前局部话题及其与原帖的关系，减少小模型被长原帖重新带偏。
- 当前原生用户消息和直接回复对象始终保留原文，不由压缩结果替代；压缩输出按原帖/楼层分区并标记为不可信背景，可信焦点规则移动到文字和图片上下文之后作为最终路由约束。
- 原帖 1600 字、最近 12 条且单条最多 800 字的物理窗口保留为短楼层、关闭压缩或压缩失败时的确定性兜底；压缩正常触发时改用更宽但硬限制的输入窗口（原帖最多 8000 字、最近楼层合计最多 8000 字），避免在压缩前就丢掉早先发生的话题迁移。
- 语义压缩默认只在可压缩背景超过 2400 字时触发；Provider 缺失、超时、异常或 JSON 无法解析均直接回退确定性路径，不阻塞正常回复。主动刷帖不应用被动楼层预算或语义压缩，继续以完整帖子为主要话题。
- 被动楼层图片改为强制按来源预处理：依次尝试固定图片 Provider、固定 LLM Provider、当前会话 Provider；原帖图片只允许以默认最多 800 字的低优先级视觉摘要进入最终回答模型，全部预处理失败时直接移除原帖原图。当前评论自己的图片保持最高相关性，只有全部预处理失败时才允许原图走 AstrBot 原生视觉兜底。
- 图片 LLM 预处理改为复用事件已有的图片额外回复宽限并设置单事件 60 秒硬上限；AstrBot 4.26.2+ 的预处理请求同时关闭内部多次请求重试，任一 Provider 卡住时到达总预算即取消预处理并进入既有降级路径，避免顺序 fallback 挤占后续 Grok/其他工具和最终 Agent 的回复时间。无图片事件不会创建该预算或增加 Provider 调用。
- 新增 `context.thread_reply_post_chars`、`context.thread_reply_recent_comments`、可选 `providers.context_provider_id` 以及楼层压缩开关、触发阈值、输出预算和超时配置；留空上下文 Provider 时依次复用固定 LLM Provider、当前会话 Provider。
- 保持多人 UID、共享楼层 session、Grok 图片隔离、Agent/分段回复聚合、主动刷帖与其他平台行为不变；未安装 Grok 不影响本功能。
- 数据库迁移版本保持 v6，无需迁移现有账号、通知游标、审核记录或会话映射。

## v1.2.11 - 2026-08-06

- 同一帖子/楼层继续共享一个 AstrBot 会话，但每一轮小黑盒用户消息都会把真实发送者 UID 作为非临时内容写入 LLM 会话历史，避免历史中的多个 `role=user` 被模型误认为同一个人。
- 当前触发发言人 UID 同时进入可信运行时元数据，并明确约束不同 UID 的第一人称归属；A、B、C 可以接着讨论，但“我/我的”不会因为共享楼层上下文而自动串人。
- 完整帖子与楼层背景继续使用 `TextPart.mark_as_temp()`，不会因为身份持久化而把整段动态社区上下文重复写入历史。
- 保持现有确定性 session、回复路由、Grok 图片隔离和 Agent 最终回复聚合不变；QQ 等其他平台不注入小黑盒身份标签。
- AstrBot 包级契约新增 `get_sender_id()` 检查，并增加同楼层不同发送者、持久化身份标签及其他平台零注入回归测试。

## v1.2.10 - 2026-08-06

- 修复带图小黑盒事件调用 `grok_web_search` 时，Grok 插件自动重复提取原图并把网页查询带偏成图片描述的问题。
- 普通 Grok 网页查询仅在工具执行期间临时隔离小黑盒原图，工具返回后立即按原顺序恢复；其他平台和其他工具不受影响。
- 明确搜图、识图、图片出处等查询继续保留 Grok 原图能力；显式传入 `image_urls` 时只隔离事件中的重复原图。
- Agent 完成阶段增加图片恢复兜底，工具异常或回调缺失也不会让事件消息链永久丢图。
- 仅在实际调用 `grok_web_search` 时给该次查询追加直接回答约束，要求结果围绕实时事实、失败时明确说明，避免返回图片描述或“稍后再查”占位语；未安装 Grok 和其他工具完全不注入该约束。
- 包级契约新增 `on_using_llm_tool` / `on_llm_tool_respond`，并继续覆盖最低、重点及当前稳定 AstrBot 版本。

## v1.2.9 - 2026-08-04

- 小黑盒回复改为跟踪 AstrBot Agent 的开始与完成状态，工具调用状态和中间提示不再抢占最终评论。
- 无工具调用的普通 LLM 回复继续在 Agent 完成后正常提交，不依赖必须出现工具调用。
- `grok_web_search` 等多步工具链会等待最终模型回复，再一次性提交小黑盒；AstrBot 自带分段回复和流式聚合继续兼容。
- 插件在进入 Agent 前直接发送、随后又要求继续调用 LLM 时，业务结果会暂存并与最终回复去重合并，最终回复优先保留。
- 包级契约新增 `on_agent_begin` / `on_agent_done`，已核对 AstrBot 4.24.2、4.26.2 和 4.27.1。

## v1.2.8 - 2026-08-03

- 主动刷帖支持显式关闭模拟运行和人工审核，AI 生成的回复可直接真实发布到小黑盒。
- 移除无审核真实发送组合的启动期配置异常，避免用户选择该模式后导致整个插件加载失败。
- `proactive_feed.dry_run` 与 `review_required` 现在独立参与运行时分流：模拟、人工审核和无审核直发均按配置执行。
- 无审核直发复用现有事件发送记录、楼层锁、重复发送闸门、发送状态未知核对和主动额度统计，并在启动时记录高风险警告。
- 保持主动刷帖、模拟运行和人工审核的安全默认值不变；配置界面与 README 增加组合行为和风险说明。

## v1.2.7 - 2026-08-02

- LLM 上下文新增独立的可信运行时元数据块，明确区分作者发帖时间、触发内容发布时间、插件发现/读取时间和 AI 开始生成时间。
- 主动浏览事件明确标注“没有作者新评论触发”，并提供帖子与触发内容在插件读取时已经发布多久。
- 增加时间解释硬约束：禁止把插件读取或 AI 处理时间归因给作者，不得据此推断作者在线、刚发帖、熬夜或早起。
- 楼层历史评论逐条附带发布时间；缺失时间明确显示“未知”，不再让模型自行猜测。
- 通知持久化同时保存源内容时间与插件发现时间，重试或重启恢复后继续保持两者语义，不再用发现时间覆盖评论时间。

## v1.2.6 - 2026-08-02

- `max_per_day` 完全改为每日主动 AI 生成请求上限：浏览推荐流和本地过滤不再消耗额度，仅合格帖子即将提交给 AI 时原子计数。
- `max_per_run` 对应每轮主动 AI 请求上限；达到每日额度后停止主动生成，被动回复、审核和发送流程保持不变。
- 数据库 v6 迁移会清零旧版按浏览量记录的主动计数，避免升级当天误占新的 AI 请求额度。
- WebUI 事件类型、处理状态和筛选项完成中文化。
- WebUI 登录、轮询、事件、清理等时间统一转换为上海时区的 `YYYY-MM-DD HH:mm:ss` 格式。

## v1.2.5 - 2026-08-01

- 修复远端发布链路的 UTF-8 编码破坏，恢复仓库中文源码、文档、元数据和 Release 正文。
- 管理页改为按标签懒加载：首次打开不再预取事件、候选、日志和存储统计，离开日志页后释放 SSE 订阅。
- 高频 SSE 日志改为按浏览器渲染帧合并刷新，减少重复 DOM 重绘和页面 CPU 占用。
- HTTP 客户端池增加并发初始化闸门，并复用存活客户端，避免重复读取凭证和竞态创建连接池。
- 仓库校验忽略 Codex 附件与测试临时目录，恢复打包体积和运行产物检查的可重复性。
- 将远端保留的推荐流 fixture、管理页交互 harness 与本地源码统一纳入版本控制。

## v1.2.4 - 2026-08-01

- 修复主动刷帖将管理页分区值误传为推荐流 `source` 参数，导致小黑盒返回 `pull 参数不正确 (0/1)` 并触发冷却熔断。
- 主动推荐流恢复已验证的 `app=heybox`、`pull=0`、`offset` 接口契约；不再发送 `source`、`cursor` 或 `limit`。
- 分区选择恢复为全部、PC 游戏、手机游戏、主机游戏、盒友杂谈、盒友日常、数码科技、动漫二次元、影视娱乐、电竞赛事、游戏攻略、优惠资讯和独立游戏，并按返回的主题标签本地筛选。
- 恢复主动帖子图片、热度和主题字段的规范化，补充接口参数与本地分区筛选回归测试。

## v1.2.3 - 2026-08-01

- 事件记录改按处理完成时间显示、筛选和排序；已发送评论显示实际发送完成时间，不再显示原帖发布时间。
- 小黑盒上下文新增原帖发布时间、触发回复时间和当前回复处理时间，模型可据此判断时效。
- `max_per_day` 改为每日主动浏览上限，读取推荐流即计数，额度耗尽后停止读取；审核批准和真实发送不再消耗该额度。
- 主动帖子来源恢复为推荐流分区选择，支持全部、游戏、硬件、软件、电竞、动漫、影视、音乐、生活和科技。
- 固定 LLM Provider、固定图片 Provider 改为 AstrBot 已配置 Provider 的下拉选择，留空继续跟随当前配置。

## v1.2.2 - 2026-07-31

- 修复主动审核真实发送没有回写原事件的问题，事件记录现在会显示主动回复的最终状态和内容。
- 主动帖子事件改用实际发现时间，不再使用原帖创建时间；数据库迁移会修正已有主动事件的历史时间标记。
- 事件记录增加“已生成”状态筛选，并保存主动候选与原事件的关联。
- 修复移动端主动审核“批准”按钮无法确认，改用页面内确认弹窗并区分模拟运行结果。
- 增加固定 LLM Provider 和固定图片 Provider 选择；留空时继续跟随 AstrBot 当前配置。
- 固定图片 Provider 失败时保留原图并回退 AstrBot 原生图片流程，避免图片上下文静默丢失。

## v1.1.3 - 2026-07-30

- 多图事件按实际图片数量自动扩展 AstrBot 原生推理截止时间，6 图默认由 120 秒扩展为 300 秒。
- 保留 900 秒硬上限，并在事件元数据和超时错误中记录实际截止时间。

## v1.1.2 - 2026-07-30

- 设置保存成功后显示明确提示。
- 评论区 @ 同时合并当前评论、原帖正文、楼层上下文及双方图片。
- 保存分段前的模型完整文本，并聚合任意数量的 AstrBot 分段，避免评论截断。

## v1.1.1 - 2026-07-30

- 修复 AstrBot 覆盖更新后适配器实例未自动重建、轮询未恢复的问题。
- 平台适配器使用仓库根目录 `logo.png` 作为图标。

## v1.1.0 - 2026-07-30

- 到期重试由 SQLite 主动恢复，队列拥塞时先持久化再延后处理。
- 主动审核增加原子发送状态和并发闸门，避免重复批准产生多条评论。
- 完成 AstrBot 4.26.8、更新数据继承和参考项目逻辑复核。

## v1.0.10 - 2026-07-30

- 修复升级或重启后处理历史 @ 的问题，按账号和通知类型持久化最新 `message_id`。
- 修复同一事件重复回复：发送前检查 SQLite 发送记录，明确失败转为终态，发送状态未知进入核对。
- SQLite 迁移升级至 v4；同步更新测试、README 和兼容性文档。

## v1.0.9 - 2026-07-30

- 修复历史通知重复占用队列和单用户积压问题。
- Plugin Page 设置改为中文分组表单，统一使用“模拟运行”文案。

## v1.0.8 - 2026-07-29

- 兼容评论 @ 的 `message_type=17`，完善帖子级和楼层级会话映射。
- 增加真实响应形状 fixture、可视化设置页和接口兼容性检查。

## v1.0.7 - 2026-07-29

- 完善 Web 扫码登录参数、稳定设备身份和 `x_xhh_tokenid` 持久化。
- 调整登录失败恢复与熔断清理。

## v1.0.6 - 2026-07-29

- 补充认证请求动态 `hkey`，账号检查改用已观察接口。
- `relogin` 统一按凭证失效处理。

## v1.0.5 - 2026-07-29

- 识别 HTTP 200 中的业务失败状态。
- 增加通知响应形状诊断和剪贴板权限降级。

## v1.0.4 - 2026-07-28

- 修正通知分页、消息类型、评论路由和 Workshop 评论字段。
- 增加帖子富文本与图片解析。

## v1.0.3 - 2026-07-28

- 修复 AstrBot 日志缺少 `plugin_tag` 导致管理页加载失败。
- 完善二维码状态清理。

## v1.0.2 - 2026-07-28

- 扩展扫码成功响应的 UID 与凭证解析。
- 增加登录字段脱敏诊断。

## v1.0.1 - 2026-07-28

- 兼容更多二维码确认状态。
- 修复自动清理临时失败后的任务恢复。

## v1.0.0 - 2026-07-28

- 首次发布：原生平台适配器、扫码登录、通知轮询、模拟运行、SQLite 和 Plugin Page。
