# 更新日志

## v1.0.6 - 2026-07-29

- 修复小黑盒认证请求缺少动态 `hkey`，Python 实现依据 MIT 许可的 heybox-core，并保留第三方声明。
- 账号有效性检查改用已观察到的 `/bbs/app/api/user/permission`，移除未验证的 `/account/info/` 假设。
- `status/stat=relogin` 现在按凭证失效处理，立即更新账号状态、打开熔断并停止轮询，不再让页面保留错误的 `success`。
- 认证请求补充网页版本参数；Workshop `_rnd` 仍未真实验证，真实评论发送继续保持关闭。

## v1.0.5 - 2026-07-29

- 补齐认证请求的网页客户端环境参数；不复制参考项目的私有签名常量。
- HTTP 200 中的 `status/stat=failed` 不再被误判为轮询成功，管理页会显示脱敏失败原因。
- 状态总览和脱敏诊断增加 @/回复通知的原始条数、接收条数、消息类型和响应形状。
- 轮询日志在通知结构变化时输出一次安全摘要，不记录 UID、昵称、正文、Cookie 或 Token。
- Clipboard API 被 Plugin Page 权限策略禁止时，诊断内容会显示在只读文本框中供手动复制。
- 修复清理统计中的 `session_mappings` 被过度脱敏为 `[REDACTED]`。

## v1.0.4 - 2026-07-28

- 修复通知轮询请求契约：@ 使用 `message_type=16`，评论/回复使用 `list_type=0` 并筛选消息类型 1/2，分页改为 `offset/limit/no_more`。
- 兼容 `result.messages`、顶层结果数组以及 `comment_a_id`、`comment_a_text`、`linkid`、`userid_a` 等通知字段。
- 回复路由的父评论改为当前触发评论 ID，避免把机器人回复错误地挂到上一层评论。
- 解析帖子 JSON 富文本中的文字、HTML 与图片段，继续交由既有清洗和图片安全流程处理。
- 评论发送契约改为 Workshop 主机和表单字段，并兼容顶层 `commentid` 成功响应；签名细节仍待真实账号验证。

## v1.0.3 - 2026-07-28

- 修复插件标准库日志记录直接传播到 AstrBot 根格式器时缺少 `plugin_tag` 字段，导致管理页状态接口返回失败的问题。
- 插件文件日志现在与 AstrBot 根日志隔离；控制台日志改由 `astrbot.api.logger` 发出，保留 AstrBot 要求的记录字段。
- 增加日志隔离回归测试，防止状态刷新再次被日志格式化异常中断。
- 成功登录、登出或登录会话消失后，管理页立即移除旧二维码及其图片数据。

## v1.0.2 - 2026-07-28

- 兼容扫码成功响应直接返回 `heyboxid`、`pkey`、`profile` 和
  `account_detail.userid`，不再假定 UID 必然来自 `Set-Cookie`。
- 兼容扫码状态 `wait`、`ready`、`ok`。
- 登录成功但凭证不完整时，通过同一匿名会话调用 `restore_login` 补齐凭证。
- 将“缺少 UID”和“缺少登录凭证”拆分为明确错误，并只记录字段名与数量等脱敏诊断。

## v1.0.1 - 2026-07-28

- 修复真实扫码流程确认后仍停留在等待状态：兼容 `result.error = "ok"`，
  原样传递二维码 URL 查询参数，并从完整 Cookie 会话读取 UID。
- 兼容 `qr_url`、`expire`、`error_msg` 等参考项目实际使用的字段。
- 修复自动清理遇到临时 SQLite 错误后永久退出；现在记录脱敏原因并在一小时后重试。
- 清理关联失败回复时先删除子记录，并关闭 SQLite 查询与维护游标，降低锁冲突风险。

## v1.0.0 - 2026-07-28

- 首个版本：小黑盒原生平台适配器、扫码登录、通知轮询、确定性会话、dry-run、主动刷帖审核、SQLite 数据生命周期和 Plugin Page。
- 所有未经过真实小黑盒账号验证的接口字段均集中在解析层并在文档中标注。
